<div data-deck-target="<%= @side %>" class="<%= @class_name %>">
  <div class="fixed inset-0 flex flex-col justify-between items-center">
    <div class="flex justify-between items-center w-full">
      <div></div>
      <%= render UI::Atoms::IconLink.new(
        href: :root,
        icon: "x",
        size: :md,
        color: :white
      ) %>
    </div>
    <div class="text-3xl font-semibold">
      <%= content %>
    </div>
    <div class="grid grid-cols-2 gap-4 w-full p-4">
      <div>
        <%= button_left %>
      </div>
      <div>
        <%= button_right %>
      </div>
    </div>
  </div>
</div>