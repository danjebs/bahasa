<div class="h-full">
  <div class="container m-auto flex flex-col items-center">
    <h1>
      <%= "Your #{@journey.language.name} Journey" %>
    </h1>
    <div class="flex flex-col gap-4">
      <%= render UI::Atoms::LinkButton.new(
        href: new_deck_path,
        label: "Daily Practice",
        size: :lg
      ) %>
      <%= render UI::Atoms::LinkButton.new(
        href: continue_path,
        label: "Continue",
        size: :lg
      ) %>
      <%= render UI::Atoms::LinkButton.new(
        href: new_lesson_path,
        label: "Add New Lesson",
        size: :lg
      ) %>
      <%= render UI::Atoms::LinkButton.new(
        href: lessons_path,
        label: "Lesson List",
        size: :lg
      ) %>
    </div>
  </div>
</div>