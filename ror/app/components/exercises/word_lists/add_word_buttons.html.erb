<turbo-frame id="<%= dom_id(@word_list) %>_new_exercise_word_frame">
  <div class="flex justify-end py-2 px-3">
    <%= render(LinkButton.new(
      label: "Add Word",
      href: new_exercise_word_path(exercise_id: @word_list.id),
      data: { turbo_frame: "#{dom_id(@word_list)}_new_exercise_word_frame" },
      size: :sm
    )) %>
  </div>
</turbo-frame>
