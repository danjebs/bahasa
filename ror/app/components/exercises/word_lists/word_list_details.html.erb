<div class="border border-solid border-gray-100 dark:border-gray-600 rounded-lg overflow-clip">
    <div class="flex w-full font-semibold bg-gray-50 dark:bg-gray-600">
      <div class="w-48 py-2 px-3">
        English
      </div>
      <div class="w-48 py-2 px-3">
        <%= @word_list.language.name %>
      </div>
      <div class="">
      </div>
    </div>

  <turbo-frame
    id="<%= dom_id(@word_list) %>_list_frame"
    data-controller="reorderable"
    data-reorderable-url="<%= word_path(':id') %>"
  >
    <%= render(Words::WordListing.with_collection(@word_list.words.ordered)) %>
  </turbo-frame>
</div>

<%= render Exercises::WordLists::AddWordButtons.new(word_list: @word_list) %>
