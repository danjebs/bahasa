<%= form_with(model: @word_list, class: "contents") do |form| %>
  <%= render Forms::ErrorNotice.new(model: @word_list) %>

  <%= render Forms::TextField.new(form: form, field: :title) %>

  <%= render Forms::FormActions.new(form: form) %>
<% end %>
