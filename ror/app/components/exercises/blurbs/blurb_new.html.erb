<%= render Forms::Form.new(
  model: @blurb,
  cancel_href: @blurb.lesson,
  delete_href: @blurb.becomes(Exercise),
  fields: [
    { name: :title, type: :text },
    { name: :content, type: :rich_text },
    { name: :lesson_id, type: :hidden, value: @blurb.lesson_id }
  ]
) %>