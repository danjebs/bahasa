<turbo-frame id="remote_modal">
  <%= render UI::Layout::Modal.new(id: dom_id(@phrase_list)) do |component| %>
    <% component.with_heading do %>
      Edit Phrase List
    <% end %>
    <%= render Forms::Form.new(
      model: @phrase_list,
      fields: [{ type: :text, name: :title }],
      cancel_href: @phrase_list.lesson,
      delete_href: @phrase_list.becomes(Exercise)
    ) %>
  <% end %>
</turbo-frame>
