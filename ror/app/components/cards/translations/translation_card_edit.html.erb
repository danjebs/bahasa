<turbo-frame
  id="<%= dom_id(@translation_card) %>"
  data-model="card"
  data-id="<%= @translation_card.id %>"
>
  <%= render Cards::Translations::TranslationCardForm.new(translation_card: @translation_card) %>
</turbo-frame>