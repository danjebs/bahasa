<div class="flex justify-between items-center mb-6">
  <h1 class="font-bold text-4xl">
    Lessons
  </h1>
  <%= render LinkButton.new(
      href: new_lesson_path,
      label: "New Lesson",
    )
  %>
</div>
<div
  id="<%= params[:lang] %>_lessons"
  class="min-w-full rounded-lg shadow-md border border-solid border-gray-100"
  data-controller="reorderable"
  data-reorderable-url="<%= lesson_path(':id') %>"
>
  <% @lessons.ordered.each do |lesson| %>
    <%= render Lessons::LessonListing.new(lesson: lesson) %>
  <% end %>
</div>
