<%= form_with(model: @lesson, class: "contents") do |form| %>
  <%= render Forms::ErrorNotice.new(model: @lesson) %>

  <%= render Forms::TextField.new(form: form, field: :title) %>

  <%= render Forms::FormActions.new(form: form, cancel_href: @lesson) %>
<% end %>
