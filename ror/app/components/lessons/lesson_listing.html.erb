<%= link_to @lesson, id: dom_id(@lesson), class: "flex p-4 last:border-none border-b border-solid border-gray-200 md:border-none" do %>
  <div>
    <div class="text-base text-gray-500">
      <%= "Lesson #{@lesson.position}" %>
    </div>
    <div class="text-xl font-semibold">
      <%= @lesson.title %>
    </div>
  </div>
<% end %>